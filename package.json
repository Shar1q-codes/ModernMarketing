{
  "name": "modern-marketing",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm --dir web run build",
    "dev": "pnpm --dir web run dev",
    "start": "pnpm --dir web run start",
    "lint": "pnpm --dir web run lint"
  }
}
